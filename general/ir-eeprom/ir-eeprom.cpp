#include <fstream>
#include<iostream>

using std::cout;
using std::ofstream;
using std::endl;

int main(int argc, char* argv[])
{
    if (argc < 3)
    {
        cout << "Too few arguments" << endl;
        return -1;
	}
}